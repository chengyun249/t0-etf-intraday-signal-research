# Legacy sample outputs

This directory contains outputs produced before the v2 execution-model fixes.
They are retained only to show report schemas and must not be compared directly
with new runs. New results belong under `results/` and must record the code
version, cost grid, stop slippage, universe rule, and sample dates.
